% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perturbMatch_class.R
\name{topRegulators}
\alias{topRegulators}
\alias{topRegulators,PerturbMatch-method}
\title{Top-scoring regulator signatures}
\usage{
topRegulators(object, n = 5, ...)
}
\arguments{
\item{object}{A \code{PerturbMatch} object.}

\item{n}{Number of signatures to return, capped at \code{nrow(object)}.
Default \code{5}.}

\item{...}{Additional arguments passed to methods.}
}
\value{
A \code{data.frame} with at most \code{n} rows, ordered by descending
  absolute \code{score}.
}
\description{
Returns the \code{n} highest-scoring rows of \code{\link{regulatorTable}},
ranked by
absolute enrichment score.
}
\examples{
mat <- matrix(runif(5), nrow = 5, dimnames = list(
    paste0("GENE", seq_len(5)), "score"
))
se <- SummarizedExperiment::SummarizedExperiment(
    assays = list(enrichment = mat)
)
S4Vectors::metadata(se) <- list(
    up_regulated   = "GENE1",
    down_regulated = "GENE5",
    background     = paste0("GENE", seq_len(5))
)
pm <- new("PerturbMatch", se)
topRegulators(pm, n = 3)
}
